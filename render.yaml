services:
  - type: web
    name: vcp-core
    env: docker
    plan: starter
    dockerfilePath: ./Dockerfile.vcp
    healthCheckPath: /
    autoDeploy: true
    envVars:
      - key: TESLA_PRIVATE_KEY_PEM
        sync: false   # syötä arvo Renderin dashboardissa (salainen PEM, ECDSA P-256)
